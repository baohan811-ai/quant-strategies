# PJ1 项目协作规则

## 项目知识与优先级

- 处理量化策略、回测、行情、基本面、数据库或 Wind 任务前，先阅读 `PROJECT_NOTES.md` 的“重要提醒”和与当前任务相关的章节。
- `PROJECT_NOTES.md` 是项目级策略结论、数据口径和运行状态的唯一长期记录。带日期的数量、进度和状态属于历史快照，执行前必须重新核实现状。
- 数据准确性、point-in-time 正确性和可审计性，高于 Wind 额度、运行速度和开发便利性。不得通过混用口径、降低完整性标准或静默降级来让正式策略继续运行。
- `.md` 文件默认谨慎维护；除非用户明确要求，不修改现有 Markdown，也不在策略目录新增零散记录。值得长期保存的结论应建议写入 `PROJECT_NOTES.md`，获得确认后再修改。

## 数据与回测底线

- 正式策略只能使用当前已验收为 `ready` 的数据模型；`building`、`updating`、`incomplete` 或 `invalid` 状态不得绕过或伪装为可用。
- 财务因子必须能说明报告期、公告日、策略可用日、来源和计算口径，并硬性保证 `available_date <= signal_date`。无法证明历史当时可见的数据不得用于正式回测。
- A 股行情统一通过项目共享读取层获取，并按目标股票池、字段和日期层级检查覆盖率；不要让脚本自行查询底层表，或以独立 pkl、临时 Wind 拉取静默替代权威数据源。
- 完整 EOD 和盘中实时数据必须隔离：正式回测使用完整 EOD；当日实时数据仅可在内存中临时叠加，不写入正式历史库。
- 原始价格、复权因子和公司行为必须保持一致版本。不得滚动拼接不同抓取时点的供应商前复权价格；实际成交与持仓由不可回写的交易台账维护。
- 回测和策略审计必须检查历史股票池、幸存者偏差、未来函数、停牌与退市、复权与公司行为、涨跌停和成交可行性、交易成本及信号到下单的时间关系。

## 研究、修改与验证

- 先区分研究脚本、开发版和执行版。修改执行版的参数、过滤器或买卖规则前，先在临时实验或独立研究版本中做基准对比；只有用户明确要求落地，或用户确认了稳定改善的方案，才修改正式脚本。
- 临时探测、参数试验和一次性验证脚本放在 `/tmp` 等临时目录；项目内只保留用户明确要求的正式脚本或长期维护工具。
- 参数研究优先使用可复现的枚举或网格搜索并保留关键中间结果。选择参数时检查起始日扰动、参数邻域、稳定平台和样本外表现，不追逐孤立尖峰。
- 比较方案时固定数据版本和口径，至少报告收益、年化收益、夏普、最大回撤、换手或交易数、成本假设和关键稳定性结果；数据口径改变后必须重跑基准，必要时明确旧结论失效。
- 修改后按风险验证语法、关键路径、数据质量门槛和输出文件。保留用户现有改动，不格式化或改写无关文件。

## Wind 与数据库维护

- 预计大量调用 Wind 前，先向用户报告拟补数据、日期范围、标的数、字段数、预计请求量、校验方法和可拆分批次，由用户确认额度后再执行。
- 开跑前确认 WindPy 已登录、终端可用；补数应支持断点续跑并遵守已确认额度。限额、权限失败或稳定空结果必须显式记录，不能把部分完成描述为完整。
- 补数结束后输出完成度表，至少包含数据内容、范围、标的数、成功与失败/跳过批次、非空量、完成度和剩余问题。

## 沟通方式

- 用户希望在协作中学习专业量化流程。回答应先给结论，再用简洁、可执行的方式说明证据、风险和专业检查建议，不用术语堆砌。
- 诊断请求以查明原因和给出证据为止；用户未要求修复时不要顺带修改。变更请求则完成必要实现和成比例验证。
